# API Specifications

API interface definitions, both machine-readable and human-readable.

## Format

- REST APIs: OpenAPI/Swagger YAML files (`.yaml` or `.yml`)
- GraphQL: Schema definition files (`.graphql` or `.gql`)
- gRPC: Protocol buffer files (`.proto`)
